# ByteTrue Deep Dive

[返回 README](../README.md)

这份文档承载较长的工作流图、运行时目录结构、设计哲学和近期 Roadmap，避免顶层 README 膨胀。

---

## 工作流示意

ByteTrue 的技能不是一条线性流水，而是**分层 + 事件驱动**的：

```
═══════════════════════════════════════════════════════════════════════
 根入口 · 路由                              （任何时刻都可以调用）
───────────────────────────────────────────────────────────────────────
   bt ──▶ 介绍体系 / 把开放式诉求路由到下面任一具体子技能
          （本身不做事，只做分诊和提示）
═══════════════════════════════════════════════════════════════════════
                              │
              ┌───────────────┼───────────────┐
              ▼               ▼               ▼
        （未接入）        （已接入）      （想了解体系）
         走阶段 0       直达 1~4 层 / 横切    给速读
              │
              ▼
═══════════════════════════════════════════════════════════════════════
 阶段 0 · 接入                                  （只在新项目跑一次）
───────────────────────────────────────────────────────────────────────
   bt-onboard ──▶ 生成 .bytetrue/ 骨架 + 释放 reference/、tools/
═══════════════════════════════════════════════════════════════════════
                              │
                              ▼
═══════════════════════════════════════════════════════════════════════
 第 1 层 · 长效档案（“系统现在长什么样”，只记现状）
───────────────────────────────────────────────────────────────────────
   bt-req   ──▶ .bytetrue/requirements/{slug}.md
   bt-arch  ──▶ .bytetrue/architecture/ARCHITECTURE.md
                                       └─ {type}-{slug}.md（子系统）
═══════════════════════════════════════════════════════════════════════
                              │
                              ▼
═══════════════════════════════════════════════════════════════════════
 第 2 层 · 规划（“接下来打算怎么做这块大需求”，大需求才需要）
───────────────────────────────────────────────────────────────────────
   bt-roadmap ──▶ .bytetrue/roadmap/{slug}/
                  把一个“我想要 X 系统”做成完整的事前规划：
                    ① 概设          —— 拆成哪几个模块 / 组件
                    ② 架构层详设    —— 模块间接口契约 / 共享协议
                    ③ 子 feature    —— 把方案分解成多条可执行的 feature
                  ② 是 feature-design 的硬约束输入
                  （小需求可跳过本层，直接进第 3 层）
═══════════════════════════════════════════════════════════════════════
                              │
                              ▼
═══════════════════════════════════════════════════════════════════════
 讨论入口（可选 · 想法模糊或方案需要拷问时进入）
───────────────────────────────────────────────────────────────────────
                          ┌── case 1 已经够清楚 ──▶ bt-feat-design
   bt-brainstorm ────────▶├── case 2 小需求方向定 ─▶ feature 流（落 brainstorm.md）
                          ├── case 3 大需求 ready ─▶ bt-roadmap
                          └── case 4 需要问透 ─────▶ bt-grill
                              │
                              ▼
═══════════════════════════════════════════════════════════════════════
 第 3 层 · 执行流程（按事件类型选一条进入）
───────────────────────────────────────────────────────────────────────

  ▸ 事件：新增能力                                          ┌──────────┐
       bt-feat-design ──▶ bt-feat-impl ──▶ bt-feat-accept  │ features │
       bt-feat-ff     ──(轻量直通车，跳过 design/accept)─▶  │ /YYYY-…/ │
                                                            └──────────┘

  ▸ 事件：修复缺陷                                          ┌──────────┐
       bt-issue-report ──▶ bt-issue-analyze ──▶ bt-issue-fix│  issues  │
                                                            │ /YYYY-…/ │
                                                            └──────────┘

  ▸ 事件：代码腐化（beta）                                   ┌──────────┐
       bt-refactor / bt-refactor-ff                         │refactors │
                                                            │ /YYYY-…/ │
                                                            └──────────┘
═══════════════════════════════════════════════════════════════════════
                              │
                ▼ 任意阶段觉得“这个值得记下来”都能触发 ▼
═══════════════════════════════════════════════════════════════════════
 横切层 · 知识沉淀（复利工程）
───────────────────────────────────────────────────────────────────────
   bt-learn   ──▶ ┐
   bt-trick   ──▶ ├─▶ .bytetrue/compound/YYYY-MM-DD-{doc_type}-{slug}.md
   bt-decide  ──▶ │     doc_type ∈ { learning, trick, decision, explore }
   bt-explore ──▶ ┘
                   ↑
          下一次 bt-arch / bt-feat-design / bt-issue-analyze
          会回头读 compound/，让经验在新工作里被复用
═══════════════════════════════════════════════════════════════════════
```

**怎么读这张图：**

- **纵向是层次**，不是严格时间顺序——长效档案层会反复刷新，规划层只在大需求时进入
- **第 3 层是事件入口**：新需求走 feature 流，bug 走 issue 流，腐化走 refactor 流
- **横切层是飞轮**：任何流程跑完发现“这事值得记下来”都可以触发沉淀，沉淀的产物又会被下一次同类工作读到

---

## 运行时结构

`/bt-onboard` 跑完后，会在项目根下生成一个 `.bytetrue/` 目录——这是 ByteTrue 所有产物的聚合根，也是各个子技能运行时唯一会读写的工作区。

```text
你的项目/
├── .bytetrue/
│   ├── requirements/
│   │   └── {slug}.md
│   ├── architecture/
│   │   ├── ARCHITECTURE.md
│   │   └── {type}-{slug}.md
│   ├── roadmap/
│   │   └── {slug}/
│   │       ├── {slug}-roadmap.md
│   │       ├── {slug}-items.yaml
│   │       └── drafts/
│   ├── features/
│   │   └── YYYY-MM-DD-{slug}/
│   │       ├── {slug}-brainstorm.md
│   │       ├── {slug}-design.md
│   │       ├── {slug}-checklist.yaml
│   │       └── {slug}-acceptance.md
│   ├── issues/
│   │   └── YYYY-MM-DD-{slug}/
│   │       ├── {slug}-report.md
│   │       ├── {slug}-analysis.md
│   │       └── {slug}-fix-note.md
│   ├── refactors/
│   │   └── YYYY-MM-DD-{slug}/
│   ├── audits/
│   ├── compound/
│   │   └── YYYY-MM-DD-{doc_type}-{slug}.md
│   ├── tools/
│   └── reference/
│       ├── shared-conventions.md
│       ├── system-overview.md
│       ├── domain-context.md
│       └── project-management.md
└── AGENTS.md
```

几条要点：

- 所有产物聚在 `.bytetrue/` 下，让“上次那个 feature / bug 当时怎么搞的”三秒能找到
- `requirements/` 和 `architecture/` 是**长效档案**，`roadmap/` 是**规划层**，两者刻意分开
- `compound/` 是唯一的知识沉淀目录，learning / trick / decision / explore 通过 `doc_type` 区分
- `reference/` 由 `bt-onboard` 从技能包复制到项目里，是跨技能共享口径的唯一稳定位置

---

## 设计哲学

ByteTrue 与“围绕 Agent 编排”的体系走的是完全相反的方向。

- Agent 编排派认为：人越少介入越好，理想是全自动
- ByteTrue 认为：**程序员是软件编码中的在环对象**，可以对黑盒实现不了解，但对整体实现必须有所把控

软件架构必须要 **可演进**、**可观测**、**可控制**。

也许未来模型会让部分模块变得不再重要，但在当下，把需求、架构、设计、问题和知识这些要素组织好，依然是严肃软件开发的前提。

---

## 近期 Roadmap

- [ ] 继续强化 `bt-refactor`（仍处于 beta）
- [ ] 吸收 review / PR finishing 等团队协作收尾能力
- [ ] 继续打磨 `bt-tracker` 的外部协作体验
